{
  "name": "simple-orm",
  "version": "1.0.0",
  "scripts": {
    "build:tests": "rescript build",
    "test": "npm run build:tests && jest",
    "build:rescript": "rescript build",
    "build": "bun run build:rescript",
    "dev:rescript": "rescript build -w",
    "dev:app": "bun run --watch examples/NewApp.res.js",
    "dev": "bun run build:rescript && bun run format && concurrently 'bun:dev:*'",
    "format": "rescript format -all"
  },
  "keywords": [
    "rescript",
    "bun"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@graphile/logger": "^0.2.0",
    "@rescript/core": "^1.6.0",
    "concurrently": "^9.0.1",
    "pg": "^8.12.0",
    "rescript": "^11.1.4",
    "rescript-bun": "^0.5.0",
    "spacetime": "^7.6.1",
    "tsd": "^0.31.2",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@glennsl/rescript-jest": "^0.11.0",
    "nodemon": "^3.1.4"
  }
}